logging:
  config: classpath:log4j2-prod.yml

server:
  port: 9002
spring:
  application:
    name: gxconsumer
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

eureka:
  client:
    service-url:
      defaultZone: http://cubespace:CubeSpace.2015@localhost:20000/eureka/
    register-with-eureka: true
    fetch-registry: true

cubespace:
  uploadTempPath: /mnt/disk1/cloud/temp/
  restReadTimeout: 30000
  restConnectTimeout: 30000


