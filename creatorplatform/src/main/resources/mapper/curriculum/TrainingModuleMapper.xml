<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tlf.creator.dao.curriculum.training.TrainingModuleMapper">

  <resultMap id="BaseResultMap" type="com.tlf.creator.entity.curriculum.training.TrainingModule">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
    <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt" />
    <result column="deleted_at" jdbcType="TIMESTAMP" property="deletedAt" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="visible" jdbcType="BIT" property="visible" />
  </resultMap>

  <sql id="Base_Column_List">
    id, created_at, updated_at, deleted_at, name, visible
  </sql>

  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from training_module
    where id = #{id,jdbcType=VARCHAR} and deleted_at is null
  </select>

  <select id="selectAllVisible" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from training_module
    where deleted_at is null and visible = 1 order by created_at
  </select>

  <select id="selectAllNotVisible" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from training_module
    where deleted_at is null and visible = 0 order by created_at
  </select>

  <update id="deleteByPrimaryKey" parameterType="java.lang.String">
    update training_module set deleted_at = now()
    where id = #{id,jdbcType=VARCHAR}
  </update>

  <insert id="insert" parameterType="com.tlf.creator.entity.curriculum.training.TrainingModule">
    insert into training_module
    (id, created_at, updated_at,
      deleted_at, name, visible)
    values
     (#{id,jdbcType=VARCHAR}, now(), now(),
     null, #{name,jdbcType=VARCHAR}, 1)
  </insert>

  <update id="updateByPrimaryKey" parameterType="com.tlf.creator.entity.curriculum.training.TrainingModule">
    update training_module
    set created_at = #{createdAt,jdbcType=TIMESTAMP},
      updated_at = #{updatedAt,jdbcType=TIMESTAMP},
      deleted_at = #{deletedAt,jdbcType=TIMESTAMP},
      name = #{name,jdbcType=VARCHAR},
      visible = #{visible,jdbcType=BIT}
    where id = #{id,jdbcType=VARCHAR}
  </update>

</mapper>